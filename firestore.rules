rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Day-Zero: Open access for staging/development
    // TODO: Lock down before production launch
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
